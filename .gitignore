tests/outputs/
# Local MediaWiki credentials (copy from *.example)
tests/inputs/wiki_credentials.json

__pycache__/
*.pyc
venv/

# Local working notes, not for the public repo
CLAUDE.md

# Personal machine-bootstrap scripts, not part of the project
/setup/

# Wikipedia article drafts, not for the public repo until posted
/drafts/

# IG cookies / instaloader sessions, not for the repo
/conf/
data/ig_watch_state.json
data/ig_watch_wakeup.log
data/dlwm_input_queue.jsonl
data/ig_watch_cron.log
